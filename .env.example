# MAZJ environment variables.
#
# Copy to `.env.local` (git-ignored) and fill in. `.env.local` is the only file
# that should ever hold real values; never commit one.
#
# Validate what you have set without starting the app:   npm run check:env

# ---------------------------------------------------------------------------
# Site
# ---------------------------------------------------------------------------

# Absolute public origin, scheme included, NO trailing slash.
# `lib/site.ts` throws on a production build while this is unset, because every
# canonical, hreflang, og:url, sitemap entry and JSON-LD @id is built from it.
NEXT_PUBLIC_SITE_URL=

# ---------------------------------------------------------------------------
# Supabase
#
# All three come from: Supabase dashboard > Project Settings > API Keys
# ---------------------------------------------------------------------------

# Project origin, e.g. https://abcdefghijklmnop.supabase.co
# No trailing slash: supabase-js appends paths to it and a slash yields `//`,
# which PostgREST answers with a 404.
NEXT_PUBLIC_SUPABASE_URL=

# Publishable key (`sb_publishable_...`), or the legacy `anon` key.
# Safe in a browser by design: it carries no privileges of its own and every
# request it makes is subject to Row Level Security.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# 🔴 Secret key (`sb_secret_...`), or the legacy `service_role` key.
#
# THIS KEY BYPASSES ROW LEVEL SECURITY ENTIRELY. It is the database's root
# password wearing a different hat. Never prefix it with NEXT_PUBLIC_, never
# paste it into client code, never log it, never put it in a bug report.
# If it ever leaks, rotate it in the dashboard immediately.
SUPABASE_SECRET_KEY=

# Salt for hashing client IP addresses before they are stored.
#
# A bare IP is personal data under PDPL; a salted hash still supports rate
# limiting and abuse investigation while identifying nobody on its own.
# Rotating this deliberately severs the link to every previously stored hash.
#
#   generate with:   openssl rand -base64 32
IP_HASH_SALT=

# Which proxy sits in front of this deployment: vercel | cloudflare | none
#
# 🔴 Decides whether a forwarded IP header can be trusted, which decides whether
# the rate limits mean anything. It is CONFIGURATION and not detectable: every
# header that would reveal the topology is set by the same client being judged.
#
#   vercel      x-forwarded-for is OVERWRITTEN at the edge to prevent spoofing,
#               so the address is attested. x-real-ip carries the same value and
#               is what Vercel's own ipAddress() helper reads.
#   cloudflare  x-forwarded-for is APPENDED to, so its leftmost entry is the
#               client's own claim. Only cf-connecting-ip means anything, and
#               only if the origin cannot be reached directly.
#   none        Nothing trusted in front. Addresses are read but never trusted,
#               and the per-mobile / per-address limits carry the load.
#
# Unset means `none`. 🔴 SET THIS TO `vercel` WHEN THE VERCEL DEPLOYMENT GOES
# LIVE, or both rate limits run permanently in their weakest mode.
IP_TRUST_PROXY=

# ---------------------------------------------------------------------------
# Transactional email (Resend)
#
# Used by the startups & builders offer: the receipt to an applicant, the alert
# to MAZJ, and the approval or rejection.
#
# 🔴 VERIFY THE SENDING DOMAIN FIRST. That is DNS, not code. In Resend, add
# mazj.sa as a domain and publish the SPF, DKIM and return-path records it
# prints. Until those propagate, sending answers 403 and every application is
# received and decided correctly while nobody is ever told.
#
# ⚠️ Leaving these unset does NOT break the site. `server/env.ts` keeps them
# optional deliberately, so that a half-propagated DNS record can never take the
# live booking flow down over a marketing email. Instead, `sendEmail` refuses
# with a typed error naming the missing variable, `/admin/startups` renders the
# failure in orange beside the application, and `npm run check:env` warns.
# ---------------------------------------------------------------------------

# API key from resend.com/api-keys. Can send mail AS MAZJ: treat it as a
# credential, never NEXT_PUBLIC_, never logged.
RESEND_API_KEY=

# The From header. The domain must be a verified sending domain in Resend.
#   e.g.   MAZJ <hello@mazj.sa>
EMAIL_FROM=

# Where replies land. Founders DO reply to these, so a no-reply address wastes
# the warmest moment in the whole flow.
#   e.g.   info@mazj.org
EMAIL_REPLY_TO=

# Where a new-application alert is sent, one per submission, with a direct link
# into /admin/startups/<id>. Without it, applications sit unseen until somebody
# remembers to open the dashboard.
#   e.g.   info@mazj.org
EMAIL_ALERT_TO=

# ---------------------------------------------------------------------------
# Local tooling only. Never read at runtime.
# ---------------------------------------------------------------------------

# Direct Postgres connection, used by `npm run db:*` for migrations and type
# generation. Found under Project Settings > Database > Connection string.
#
# Note: the direct `db.<ref>.supabase.co` host is IPv6-only. On an IPv4-only
# network use the Supavisor pooler host instead.
SUPABASE_DB_URL=

# Supabase Personal Access Token, from supabase.com/dashboard/account/tokens.
#
# The Supabase CLI and Management API both read this variable by name, so
# `npm run db:*` picks it up automatically. Grants full control of every project
# in your account: treat it exactly like the secret key.
SUPABASE_ACCESS_TOKEN=

# ---------------------------------------------------------------------------
# Rekaz Merchant Public API
#
# From platform.rekaz.io > User Management > API Keys. The generated key is
# shown ONCE and cannot be re-read, only regenerated.
# ---------------------------------------------------------------------------

# API origin, including /api/public. No trailing slash.
REKAZ_API_BASE=https://platform.rekaz.io/api/public

# 🔴 The base64 `key:secret` pair, sent as `Authorization: Basic <this>`.
#
# THIS IS AN ADMIN-SCOPE CREDENTIAL, not a storefront key. `GET /customers`
# returns the entire customer list, which is personal data under PDPL. Handle it
# exactly like SUPABASE_SECRET_KEY: server-side only, never NEXT_PUBLIC_, never
# logged, never in a bug report. Rotate it in the Rekaz dashboard if it leaks.
REKAZ_AUTH_BASIC=

# Sent as the `__tenant` header. 🔴 TWO underscores: the Quick Start docs say
# `_tenant`, which the live API rejects.
REKAZ_TENANT_ID=
